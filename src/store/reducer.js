import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const meshSlice = createSlice({
  name: 'mesh',
  initialState,
  reducers: {},
});

export const {} = meshSlice.actions;
export default meshSlice.reducer;
